---
name: ai-context-kit
description: 自分専用の「外部脳」リポジトリを立ち上げ、運用する。AIとの会話を取り込み、そこから判断基準・知識・案件の状況を抽出して、以後のセッションの前提として読ませる。ユーザーが「外部脳」「セカンドブレイン」「ナレッジベースを作りたい」「AIに自分の前提を覚えさせたい」「会話ログを溜めたい」と言ったとき、または既存の外部脳リポジトリで知識の追加・抽出・棚卸しを行うときに使う。
---

# 外部脳（ai-context-kit）

AIとの会話を取り込み、判断基準を抽出して、以後すべてのセッションの前提にするための仕組み。

## 最優先ルール

**次の情報は絶対に書き込まない。** 人間が書き込もうとしていると判断した場合も、実行せずに指摘して確認を求める。これは他のどのルールよりも優先される。

- 所属組織の機密情報（実在の組織図・同僚の氏名・クライアント名・取引内容・社外秘の数値）
- 本人以外の第三者の個人情報
- パスワード、APIキー、トークン、その他の認証情報
- 判断に迷う場合は、書き込む前に必ず人間に確認する

## 使う場面の判定

| 状況 | やること |
|---|---|
| 外部脳をこれから作る | → 「立ち上げ」へ |
| 既存の外部脳に新しいことを書く | → 「書き込み」へ |
| 会話ログから知識を抽出する | → 「抽出」へ |
| 古くなっていないか確認する | → 「棚卸し」へ |

## 立ち上げ

1. `git init` して、このキットの構造を置く
2. `git config core.hooksPath .githooks` でフックを有効にする（**クローンに引き継がれないので環境ごとに必要**）
3. **`areas/self/vision.md` を最初に書く。** 「何のためにやるのか、どうなったら成功か」
4. 全プロジェクト共通の指示ファイルから、このリポジトリを判断基準の正典として参照させる

**3番を飛ばさない。** vision がないと、会話ログを溜めても何を抽出すべきか判断できない。
本人がうまく言葉にできない場合は、**喋ってもらって書き起こす**。質問で引き出してよい。

## 書き込み

### どこに書くか

| 種類 | 書き込み先 |
|---|---|
| 「今回に限らず覚えて」と明示された常設指示 | `areas/self/working-with-ai.md` |
| 判断基準・好み・こだわり | `areas/self/thinking-principles.md` |
| 継続案件の状況 | `areas/threads/` |
| 用語・概念 | `areas/glossary/` |
| 環境・ツールの意思決定 | `areas/dev-environment/` |

**細かい観察までは書かない。** 会話ログに原文が残るので後から抽出できる。

### 書き込むときの手順

1. まず `index.md` を確認する。関連する既存ファイルがあれば追記、なければ新規作成
2. フロントマター（`title` / `area` / `tags` / `created` / `updated`）を付ける
3. **`index.md` と `log.md` を同じコミットで更新する**
4. 確信度が低い・大きな構造変更を伴う場合は、ブランチを切って PR にする

### 重複の判定

**同じ話題が2箇所にあってよいのは、抽象度が違うときだけ。**

1. 片方が「どこを読めばよいか」を指しているだけなら、重複ではない。残す
2. 両方が同じ粒度で同じ事実を書いているなら、**詳細側を正典**にして概要側はリンクに置き換える
3. 抽象度が違っても、**上の層は単体で読んで意味が通ること**

**上の層に事実を書き写した瞬間に、それは腐り始める。**

## 抽出

会話ログ（`raw/`）から `areas/` に知識を引き上げる作業。

**抽出対象は「判断の理由」だけではない。** 次を含む。

- 判断の理由・判断基準
- **抽象化された知識・知恵**（分野の要点、繰り返し効く原則）
- **業務知識**（用語、制度、経緯、関係性）
- 案件の状況と論点

**やり方**

- 会話ログを引用で膨らませない。**要点を本人の言葉に近い形で1箇所にまとめる**
- 同じ話題が `raw/` に何度出てきても、`areas/` 側は1ファイルに集約・更新する
- **確信が持てないとき、抽象化の粒度に迷うときは、黙って決めずに問う。** その問答が次の精度を上げる

**抽出しても生データは消さない。** 必要に応じて元を読みに行く場面が必ずある。

## 棚卸し

`raw/` は自動で増えるが、`areas/` は更新しない限り古くなる。**古い情報はAIの推論を劣化させる。**

区切りのよいタイミングで次を確認し、**黙って直さず、何がどう食い違っているかを人間に示す。**

1. **数字・件数の陳腐化** — `areas/` の具体的な数値が今も正しいか
2. **二重管理の食い違い** — 別リポジトリの `CLAUDE.md` と `areas/threads/` に同じ技術情報がないか
3. **リンク切れ** — `index.md` の参照先が実在するか
4. **未回収の宿題** — `log.md` に「次にやる」と書いたまま反映されていないもの

### 規模の限界

索引もベクトル検索も持たないので、いずれ破綻する。次のどれかで人間に提案する。

| 指標 | 引き金 |
|---|---|
| `areas/` のファイル数 | 50を超えたら |
| `index.md` の行数 | 150行を超えたら |
| 「探したが見つからなかった」 | 同じ月に2回起きたら |

選択肢は ①`index.md` を分割 ②タグ索引を機械生成 ③検索基盤を被せる。**①から順に試す。**

## 画像を受け取ったとき

**画像そのものは保存しない**（実測で会話ログの77.7%を占め、本文が埋もれる）。

代わりに、**応答の中で読み取った内容をテキスト化する。** 基準は「**後からその画像なしで、同じ判断を再現できるか**」。エラー画面ならメッセージの文言、設定画面なら選択した項目、UIなら崩れている箇所。見た目の描写ではなく判断に効く事実を書く。

**書き出してはいけないもの**: 認証コード・ワンタイムコード、IPアドレス・所在地、ブックマークバーやタスクバーなど作業と無関係な写り込み。**画像を捨てても、そこから起こしたテキストが同じ問題を持ち込む。**

## 秘密情報が見つかったとき

取り込み時に認証情報らしき文字列は `[伏字:種別]` に置換され、報告が出る。

報告が出たら、**実物かどうかを必ず人間に確認する。** 実物だった場合、**ファイルから消すだけでは不十分で、発行元での失効・再発行が必要**（Git の履歴からは消せないため）。

`.githooks/pre-commit` が止まった場合、**`--no-verify` で回避しない。** 人間に報告して指示を仰ぐ。

## 参照時の振る舞い

- 何かを聞かれたら、まず `index.md` を見て見当をつけてから読みに行く
- 見つからない場合は正直に「見つからなかった」と伝える。**推測で答えを作らない**

## 詳細

このスキルは要約。完全な規定は同じリポジトリの `AGENTS.md` にある。
