# CONTEXT — この取り組みの全体像

> **この1枚の使い方**
> 生成AIにこのファイルを丸ごと貼ると、この取り組みの前提を共有した状態から話を始められる。
> 詳細は各リポジトリにある。AIには「必要になったらそこを読みに行く」ように扱わせる。
>
> 最終更新: 2026-09-15

---

## 何をやっているのか

**生成AIに仕事を「ツーカー」で渡せる状態を作る。**

量の話ではない。「つうと言えばかあ」——**前提が共有されていて、いちいち説明しなくても意図が通る状態**を指す。

出発点は方法論への興味ではなく、実感だった。AIは望むとおりには動かない。その原因を「指示の書き方」ではなく**前提の欠落**に置いた。だから解決策はプロンプトの工夫ではなく、**文脈の供給**になる。

やることは2つ。

1. **自分が触れる情報を取り込む**（会話、会議、チャット、メール、いずれは操作ログまで）
2. **AIが扱える形に加工する**（取り込むだけでは足りない、というのが肝）

## 今どこまでできているか

取り込みの対象を段階で分けると、**実装できているのは段階1だけ**。

| 段階 | 対象 | 状態 |
|---|---|---|
| 1 | **AIとの会話そのもの** | **実装済み**（自動取り込み） |
| 2 | 会議の書き起こし | 未着手 |
| 3 | チャット・メール | 未着手 |
| 4 | 対面の会話の録音 | 構想 |
| 5 | PCの画面・操作ログ | 構想 |
| 6 | 本人が知覚できない情報 | 構想 |

段階1でも効果はある、という仮説で進めている。**ベストではないが、一定の成果はある**という判断。

## 3つの取り組み

### 1. 外部脳（second-brain）

AIとの会話を自動で取り込み、そこから**判断基準・知識・案件の状況**を抽出して、全セッションの前提として読ませる仕組み。

構造はこう分かれている。

```
AGENTS.md    AIエージェント向けの振る舞い規定（最重要）
raw/         未加工の元データ（会話ログ。原文のまま）
areas/
  self/       思考様式・判断基準・口調  ← 中核
  people/     人間関係マップ
  glossary/   用語辞典
  threads/    継続中の案件の履歴
```

**設計の肝は3つ。**

- **生データと加工済みを両方持つ。** 抽象化した層を常用しつつ、必要なときは生データまで降りる。二者択一にしない
- **同じ話題が2箇所にあってよいのは、抽象度が違うときだけ。** 上の層は「入口とパス」を持ち、下の層が「事実」を持つ。上の層に事実を書き写した瞬間に腐り始める
- **人は毎回の判断者ではなく、キャリブレーションの相手。** 取り込みも抽出もAIが判断して実行し、人は問われたときに答え、後から訂正する

**捨てるものの方が多い。** 会話ログのうち残すのは実測で2.1%。スクリーンショットが78%、ツールの入出力が20%を占めていて、実際の発言は2.4%しかない。残すのは「どう考え、何を好み、なぜそう決めたか」が分かる情報だけ。目的はアーカイブではなく**文脈の継承**である。

→ 仕組み一式: https://github.com/feseisen/ai-context-kit

### 2. 枠の余りで自律実行する仕組み（usage-autopilot）

AIの利用枠の**使用ペースと余りを見積もり、余る分で各プロジェクトを本人の代わりに進める**仕組み。

目的は2つあり、同じ重さで扱っている。

1. **余る枠を使い切る。** リセット時点で残っている枠は消えるだけなので、その分を仕事に変える
2. **外部脳の再現度を試す。** 本人に聞かずに、外部脳に書かれた判断基準だけで「本人ならこう判断する」を実行し、どこまでトレースできたかを後から採点できる状態にする

2があるため、**成果物と同じくらい「何をどう判断したかの記録」が重要**になる。判断ログのない実行は、テストとして価値がない。

**分かった実測値**（この仕組みの副産物として、公開価値が高い部分）。

- **キャッシュが切れてからの会話の再開は、切れる前に続けた場合の約17倍**（中央値 9.35% 対 0.54%）
- 効くのは再開の頻度より**会話の大きさ**。10万トークンあたり約2.3%
- ある5時間枠では、枠の**47.8%が「再開コスト」だけ**で消えていた
- 枠を食うのは**キャッシュの書き込みと出力**。キャッシュの読み込みはほぼ使わない

→ 仕組みと実測値: https://github.com/feseisen/usage-autopilot-public

### 3. インナーソース基盤（commons）

いいものを作った人の成果を、他の人がそのまま使い回せる状態を組織の中に作る仕組み。一般名は**インナーソース（InnerSource）** — OSS のやり方を組織内部に持ち込む実践。

**効率ではなく、作り手が報われることが目的。** 車輪の再発明を避けるという効率の話から入っているが、中心に置いているのは評価の方。機能の取捨で迷ったら**帰属と評価が成立するか**を先に見る、という判断順序になる。

設計上の分岐点が2つある。

- **順位をつけない。** 作り手を順位で並べると作り手に不利益が出る。一方で「最近人気のもの」「定番」は探す側の負担を減らす。**同じ人気データを、探す側のためだけに使う**
- **使うのは素人。** ターミナルで質問に答える形や、フォルダを特定のファイルにドロップする形は分かりづらい。操作は画面で完結させる

→ 設計: https://github.com/feseisen/innersource-commons

## この取り組み全体についての判断基準

AIに渡すときに効くもの。

- **生データ主義。** 要約で置き換えない。原文を残したうえで、加工済みの層を別に持つ
- **目的と手段を入れ替えない。** 個々のルールは目的に従属する。迷ったら目的に戻る
- **粒度を細かく。** 大きな塊で渡さず、分けて渡す
- **時系列を厳守する。** 経緯は起きた順に残す
- **確信が持てないときは、黙って決めずに問う。** ただし自律実行中は止まらず、想定して進めて記録する
- **誰にでも広めるつもりはない。** 全員が使える仕組みを作ろうとしていない。意欲と理解のある層に向けた実例として示す

## 組織に広げるときの見立て（まだ設計ではなく仮説）

個人の外部脳と、組織の知識共有は**範囲が違う**。混ぜない。

情報の帰属は一次元ではなく、少なくとも3つのカットが交差する。

- **横軸（組織）**: 全社 / 事業部 / チーム
- **縦軸（職位）**
- **プロジェクト単位**（部署をまたぐので上の2つと直交する）

厄介なのは、**隣の部署が何をしているかを知っておいた方がよい場合がある**という点。境界を閉じれば済む話ではない。ただし「全部見せる」も正解ではなく、**粒度の設計が要る**。

現時点で言えそうなことが2つ。

1. **人ではなく組織に情報を紐づける。** そうすれば異動しても、所属が変われば参照先が自動的に切り替わる
2. **AIに全部を渡すのではなく、既存の置き場所（SharePoint 等）を読みに行かせる**

**組織全体の設計が完成するのを待たない。** 個人単位の個別最適から始める。これは「組織設計は要らない」という主張ではなく、順序として個人を先に置いているだけ。ただし**後で組織単位に拡張できない設計にはしない**。

## 未解決の課題

自分で挙げていて、解けていないことを承知で進めているもの。

1. PC上で動いているものを全部拾うのは、具体的にどうやるのか
2. 文書をどこまで拾うのか。実務に効くのは規則・マニュアルより「昨日作った集計ファイル」のような**日常的に生まれる大量のファイル**の方
3. **情報が最新化されない問題。** 知識ベースを最新に保つ規律をどう仕組み化するか。現状は人力の棚卸しの運用しかない
4. 現段階の生成AIの性能では処理しきれないかもしれない

4番については、**外部の進化に乗れる状態を自分の側で先に作っておく**という態度をとっている。今の性能で完結させようとしていない。

---

## AIへの指示

このファイルを読んだ上で作業するとき:

- ここに書いてあるのは**入口と概要**。詳細は上記リンク先のリポジトリが正典
- ここに書かれた判断基準を、**そのまま適用してよい**。迷ったら「目的と手段を入れ替えない」に戻る
- ここに**事実を書き足さないこと**。追記すると正典と食い違い、古い方がAIの推論を劣化させる
