# AGENTS.md — AIエージェント向け振る舞い規定

このファイルは、このリポジトリを読み書きするすべてのAIエージェント（Claude、ChatGPT、その他のコーディングエージェントを含む）が最初に読むべき「憲法」です。人間向けの説明は `README.md` を参照してください。

> **このファイルは雛形です。** `<持ち主>` などの箇所を自分の環境に合わせて書き換えて使ってください。

## 0. 最優先ルール：扱ってはいけない情報

**このリポジトリには、以下の情報を絶対に書き込まないこと。** もし人間がこれらに該当する内容を書き込もうとしていると判断した場合、AIエージェントは書き込みを実行せず、その旨を明確に指摘して確認を求めること。これは他のどのルールよりも優先される。

- 現在または過去に所属する組織の、実在する機密情報（実際の組織図、実在の同僚・上司・部下の氏名や個人情報、実際のクライアント名・取引内容・契約情報、社外秘の事業計画・数値など）
- 本人以外の第三者の個人情報（本人の同意なく特定できる氏名・連絡先・住所など）
- パスワード、APIキー、トークン、その他の認証情報（形式を問わず一切不可）
- 上記に該当するか判断に迷う場合は、必ず書き込む前に人間に確認すること

`areas/people/` `areas/glossary/` `areas/threads/` は、将来的に組織の組織図・用語辞典・取引履歴のような業務知識を管理する際の「**構造の練習**」を目的としており、**中身は本人の個人的な知識・個人の生活に関する情報に限定する**。

> **なぜ「練習」なのか。** 私用のアカウント・私用の端末で業務情報を扱うことはできない。だが構造だけは先に作れる。組織で使える形になったとき、中身を入れ替えれば動く状態にしておく、という設計。

## 0.5 このリポジトリの目的（判断に迷ったときの基準）

**幹は `areas/self/vision.md` にある。判断に迷ったらまずそこを読む。**

目指しているのは「**生成AIに仕事をツーカーで渡せる状態**」。量の話ではなく、**前提が共有されていて、いちいち説明しなくても意図が通る状態**を指す。そのために「**本人が触れる情報の全て**」を取り込み、AIが扱える形に加工することを狙う。会話ログの自動取り込みも、判断基準の抽出も、すべてその手段にすぎない。

その一部として、本人のクローンAIを作ることがある。要素は「**能力**（思考様式・判断基準・暗黙知）」と「**キャラクター性**（口調・言い回し）」の2つで、**能力を優先する**。

記録する価値の高い順:

1. 本人の判断基準・考え方・こだわり（`areas/self/`）
2. 本人が知っている知識・経緯・意思決定の理由（`areas/` の各領域）
3. 話し方の癖・口調（`areas/self/voice.md`）
4. 作ったコードそのもの（**記録しない**。リポジトリ側にあり、なくても作り直せる）

## 0.6 このリポジトリは全セッションの前提になっている

全プロジェクト共通の指示ファイル（Claude Code なら `~/.claude/CLAUDE.md`）から、このリポジトリを**判断基準の正典**として参照させる。つまり **どのフォルダで作業していても、AIは最初からここを読む前提で動く**。

そのため、他プロジェクトのセッションからも次が行われる。

- `areas/self/` と `index.md` を**読む**（作業を始める前）
- 本人について新しく分かったことを、**そのセッションのまま書き込む**（後でこのリポジトリのセッションに移すのではなく）

書き込み対象は3つに限定する。

| 種類 | 書き込み先 |
|---|---|
| 「今回に限らず覚えて」と明示された常設指示 | `areas/self/working-with-ai.md` |
| 判断基準・好み | `areas/self/thinking-principles.md` |
| 継続案件の状況 | `areas/threads/` |

**細かい観察までは書かない。** 会話ログに原文が自動で残るので、後から抽出できる。

## 0.7 別リポジトリの案件をどう扱うか

### 並列ではなく、一段上に立つ

作業フォルダの配下には別プロジェクトのリポジトリが並ぶが、**このリポジトリはそれらと対等ではない。** 全セッションの前提として読まれる（0.6）ため、**統括する側**にある。

構成も異なる。**同じ型にしてはいけない。**

| | このリポジトリ | 各プロジェクトのリポジトリ |
|---|---|---|
| 役割 | **統括**。判断基準と横断的な文脈を持つ | **実行**。成果物と、そのプロジェクト固有の事情を持つ |
| AI向けの規定 | `AGENTS.md` | `CLAUDE.md`（起動時に自動で読まれる） |
| 知識の層 | `areas/`（編集済み）＋ `raw/`（生ログ） | 成果物そのもの。`docs/` を持つこともある |
| 取り込みの仕組み | `tools/`（全プロジェクトの会話を集める） | 持たない |
| 何が集まるか | **全プロジェクト**の会話ログと、そこから抽出した知識 | そのプロジェクトの作業だけ |

### 新しいリポジトリが生まれるときの型

1. 作業フォルダの直下に**兄弟として**作る（入れ子にしない）
2. `git init`
3. `CLAUDE.md`（そのプロジェクト固有の前提・地雷・現在地）と `README.md` を置く
4. セッションをそのフォルダへ移す
5. このリポジトリの `areas/threads/` に**案件が存在すること**を1件書く

**5番が接続点。** これを書かないと、統括側から見て「そのプロジェクトは存在しない」ことになる。逆にここさえあれば、詳細は各プロジェクトの `CLAUDE.md` を読みに行けばよい。

### 役割分担の原則

- **このリポジトリで、別プロジェクトのコードを書かない。** そちらのフォルダで別のセッションを立てる
- `areas/threads/` に置くのは「**そういう案件がある**」ことと「**そこから分かった本人についての知見**」だけ
- **技術的な詳細（アーキテクチャ、既知のバグ、API設計、次のタスク）は、そのプロジェクト側の `CLAUDE.md` が正典。** ここには書かない
- 理由: 同じ技術情報を2箇所に書くと必ず食い違い、古い方がAIの推論を劣化させる
- 逆に、別プロジェクトでの作業から**本人の判断基準について新しく分かったこと**が出たら、それは必ずこちらの `areas/self/` に反映する。そこが外部脳の役割

## 0.8 同じ話題が2箇所にあってよいのは、抽象度が違うときだけ

### 層と、それぞれが持つもの

| 層 | 何を持つか | 抽象度 | いつ読まれるか |
|---|---|---|---|
| 全プロジェクト共通の指示 | 常設指示 | 指示 | **全セッションで自動** |
| このリポジトリ `areas/self/` | 本人の判断基準・常設指示の原文記録 | 高 | 作業前に読む |
| このリポジトリ `areas/threads/` | 案件が存在すること、位置づけ、**どこを読みに行けばよいかのパス** | 高 | その案件に触れるとき |
| 各リポジトリ `CLAUDE.md` | そのプロジェクト固有の前提・地雷・現在地 | 中 | そのプロジェクトのセッションで自動 |
| 各リポジトリ `docs/` | 調査結果・設計・出典・否定的検討 | 低 | 必要になったときだけ |

**上の層は「入口とパス」を持ち、下の層が「事実」を持つ。** 上の層に事実を書き写した瞬間に、それは腐り始める。

### 重複を見つけたときの判定手順

1. 片方が「**どこを読めばよいか**」を指しているだけなら、重複ではない。そのまま残す
2. 両方が**同じ粒度で同じ事実**を書いているなら、**詳細側を正典**にして、概要側はリンクに置き換える
3. 抽象度が違っても、**上の層は単体で読んで意味が通ること**。「下を読まないと何のことか分からない」概要は書かない

### プロジェクト単位の memory 機能の扱い

**原則として使わない。** 理由は2つ。

- **プロジェクトフォルダごとに分断される。** セッションを別フォルダへ移すと、それまでの記憶は読まれなくなる
- **上の層と重複しやすい。** 同じ抽象度の内容が `areas/threads/` と各リポジトリの `CLAUDE.md` の両方に重なる

使ってよいのは「**このリポジトリに書くほど固まっていない、途中の気づき**」の待避所としてだけ。固まったらこちらへ移し、memory からは消す。

### この分け方の裏付け

- **Single Source of Truth / single-sourcing**（テクニカルライティングの伝統的原則）: 正典を1つにして他は参照する。複製は必ず版ズレを起こす
- **[Anthropic「Effective context engineering for AI agents」](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)**: 全部を事前に読ませるのではなく、**ファイルパスのような軽い識別子だけ持たせ、必要になった時点で読みに行かせる**（just-in-time retrieval）
- **エージェント記憶の未解決問題**として、セッションをまたいだ同一性・時間的な抽象化と並んで **memory staleness（記憶の陳腐化）** が挙げられている。上の層に事実を書き写すのは、陳腐化を自分から作る行為

### これは仮説。検証の機会を決めておく

正解が確立している領域ではない。**次に同じ話題を2箇所に書きたくなったときが検証の機会。** そのとき上の判定手順が使えたかを `log.md` に残す。使えなければ、ルールの方を直す。

## 0.9 索引が破綻する閾値と、定期的な棚卸し

このリポジトリの検索は `index.md` を頼りにAIが読みに行く方式で、索引もベクトル検索も持たない。**小さいうちは足りるが、これは規模の問題であり、いずれ破綻する。** 壊れてから慌てないよう、見直す条件を先に決めておく。

### 見直しの引き金（どれか1つでも当てはまったら人間に提案する）

| 指標 | 引き金 |
|---|---|
| `areas/` 配下のファイル数 | **50を超えたら** |
| `index.md` の行数 | **150行を超えたら**（AIが一覧として扱えなくなる） |
| 「探したが見つからなかった」の発生 | **同じ月に2回起きたら**（数より実害が先に出る） |

引き金を引いたときの選択肢は3つ。①`index.md` を領域別に分割する ②タグの索引を機械生成する ③検索基盤を被せる。**①から順に試す**（手戻りを最小にするため）。

### 棚卸し（矛盾と陳腐化の検出）

自動同期で `raw/` は増え続けるが、`areas/` は人とAIが更新しない限り古くなる。**古い情報はAIの推論を劣化させる。**

区切りのよいタイミングで次を確認し、人間に報告する。**見つけたら黙って直さず、何がどう食い違っているかを示すこと。**

1. **数字・件数の陳腐化**: `areas/` に書かれた具体的な数値が、今も正しいか
2. **二重管理の食い違い**: 別リポジトリの `CLAUDE.md` と `areas/threads/` に同じ技術情報が書かれていないか（→ 0.7、0.8）
3. **リンク切れ**: `index.md` から参照しているファイルが実在するか
4. **未回収の宿題**: `log.md` に「次にやる」と書いたまま、どこにも反映されていないもの

## 1. 基本方針

- 新しい情報を記録する際は、まず `index.md` を確認し、関連する既存ファイルがあれば追記、なければ適切な `areas/` 配下のフォルダに新規ファイルを作成する
- ファイルを追加・更新したら、必ず `index.md` と `log.md` も同じコミットの中で更新する
- ファイル名・見出し・タグは、後で人間が自然に質問しそうな言葉遣いに合わせる（英語の技術用語より、本人が実際に使う言い回しを優先してよい）
- 完璧な分類を最初から目指さない。まず記録し、量が増えてから整理し直せばよい

## 2. ファイル形式

各ノートは冒頭にフロントマターを付ける。

```markdown
---
title: (タイトル)
area: dev-environment | people | glossary | threads
tags: [tag1, tag2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

(本文)
```

## 3. Git運用ルール

- 軽微な追記・修正（誤字修正、既存エントリへの数行の追記など）は、直接 `main` ブランチにコミットしてよい
- 内容の確信度が低い場合、大きな構造変更を伴う場合、または「扱ってはいけない情報」に該当する可能性がある場合は、ブランチを切って Pull Request を作成し、人間のマージを待つこと
- コミットメッセージは「何を」「なぜ」追加・変更したかが分かるように書く
- コミット時は `.githooks/pre-commit`（秘密情報スキャン）が走る。これが停止した場合、`--no-verify` で回避せず、必ず人間に報告して指示を仰ぐこと

## 4. `raw/` と `areas/` の役割分担

- `raw/` には、会話の文字起こしやメモなど、未加工のソースをそのまま置く。ここは基本的に編集・要約せず、原文のまま保存する
- `areas/` には、`raw/` の内容やその場の会話から抽出・整理した「加工済み」の知識を置く。同じ話題について `raw/` に複数回情報が増えても、`areas/` 側は1つのファイルに集約・更新していく

### `raw/sessions/` と `raw/memory/`（自動生成）

- 取り込みスクリプトが会話ログから生成する。**手で編集しないこと**（次回の実行で上書きされる）
- 残すのは「発言の原文」と「何をしたかの1行」だけ。ツールの出力・コードの本文・スクリーンショットは意図的に捨てている。目的はアーカイブではなく**文脈（考え方・好み・判断の理由）の継承**であり、コードは無くてもAIが作り直せるため
- ここから読み取った「本人の考え方・好み・判断基準」は、`areas/` 側の適切なファイルに抽出・集約していく。会話ログそのものを引用で膨らませるのではなく、**要点を本人の言葉に近い形で1箇所にまとめる**こと
- 取り込み時に認証情報らしき文字列は `[伏字:種別]` に置換される。伏字が報告された場合は、実物かどうかを必ず人間に確認すること。**実物だった場合、ファイルから消すだけでは不十分で、発行元での失効・再発行が必要**（Git の履歴からは消せないため）

### 添付ファイル・画像の扱い

- **画像そのものは保存しない。** 1枚で数百KBあり、実測では会話ログ全体の77.7%を画像が占めていた。保存すると本文が埋もれる
- 代わりに「🖼 画像を添付（N枚）」という1行だけを残す。完全に消すと会話の文脈が飛んで読めなくなるため
- **画像から読み取った情報は、AIが応答の中でテキスト化することで残る。**「後からその画像なしで同じ判断を再現できるか」が書き出す粒度の基準。エラー画面ならメッセージの文言、設定画面なら選択した項目、UIなら崩れている箇所。見た目の描写ではなく判断に効く事実を書く
- **書き出してはいけないもの**: 認証コード・ワンタイムコード、IPアドレス・所在地、ブラウザのブックマークバーやタスクバーなど作業と無関係な写り込み。**画像を捨てても、そこから起こしたテキストが同じ問題を持ち込む**

## 5. 検索・参照時の振る舞い

- 何かを聞かれたら、まず `index.md` を見て該当しそうなファイルの見当をつけてから読みに行くこと
- 該当するファイルが見つからない場合、正直に「見つからなかった」と伝え、**勝手に推測で答えを作らないこと**
